---
description: 指导AI使用AGENTS.md文件作为临时记忆文件，记录项目架构、需求进度、关键代码信息等。
alwaysApply: true
enabled: true
updatedAt: 2026-07-07T16:54:00.000Z
provider:
---

# Agents Memory

## 规则

AI 在对话过程中**必须使用 `AGENTS.md` 文件**作为临时记忆文件，主动记录以下信息：

| 记录类别 | 记录内容 |
|----------|----------|
| 项目架构 | 项目目录结构、模块划分、技术栈、依赖关系 |
| 需求进度 | 当前需求描述、完成状态、待办事项、阻塞问题 |
| 关键代码 | 核心函数/方法、重要算法、配置参数、API接口 |
| 决策记录 | 技术选型、设计决策、权衡考虑、替代方案 |
| 问题追踪 | 已知Bug、待解决问题、性能瓶颈、优化建议 |

## 执行流程

### 第一步：识别记录时机

在以下场景中**自动触发记录**：

1. **架构讨论**：分析项目结构、模块关系时
2. **需求分析**：理解或拆分需求时
3. **代码审查**：阅读或修改关键代码时
4. **问题解决**：调试错误、优化性能时
5. **决策制定**：选择技术方案、设计模式时

### 第二步：记录到 AGENTS.md

使用以下格式记录信息：

```markdown
## [记录类别] 记录时间

### 具体内容

- 要点1
- 要点2

### 相关代码

\`\`\`language
// 关键代码片段
\`\`\`

### 参考信息

- 文件路径: `path/to/file`
- 行号范围: 123-456
```

### 第三步：维护与更新

1. **去重检查**：记录前检查是否已存在相同信息
2. **版本更新**：更新已有记录时保留历史版本
3. **分类整理**：定期整理记录，按类别重组
4. **清理过期**：删除已完成的需求或已解决的问题

## 记录模板

### 项目架构记录

```markdown
## [架构] 2026-07-07

### 目录结构

- `/src`: 源代码目录
  - `/components`: React组件
  - `/services`: 业务逻辑
  - `/utils`: 工具函数

### 技术栈

- 前端: React + TypeScript
- 后端: Node.js + Express
- 数据库: PostgreSQL

### 模块依赖

```
前端 → API → 服务层 → 数据层
```
```

### 需求进度记录

```markdown
## [需求] 2026-07-07

### 当前需求: 用户认证系统

- [x] 登录功能
- [x] 注册功能
- [ ] 密码重置
- [ ] 第三方登录

### 待办事项

1. 实现JWT令牌刷新机制
2. 添加OAuth2.0支持
3. 完善错误处理

### 阻塞问题

- 第三方登录API文档不完整
```

### 关键代码记录

```markdown
## [代码] 2026-07-07

### 核心函数: authenticateUser

```typescript
async function authenticateUser(email: string, password: string): Promise<User> {
  // 1. 验证邮箱格式
  // 2. 查询用户记录
  // 3. 比对密码哈希
  // 4. 生成JWT令牌
  // 5. 返回用户信息
}
```

### 配置参数

- JWT_SECRET: 环境变量
- TOKEN_EXPIRY: 24小时
- SALT_ROUNDS: 12
```

## 例外

以下情况**不记录**或**特殊处理**：

1. **用户明确禁止**：用户说"不要记录"、"忽略此信息"
2. **敏感信息**：密码、密钥、个人隐私数据（需脱敏处理）
3. **临时调试**：仅用于当前调试的临时信息
4. **重复内容**：与已有记录完全相同的信息
5. **无效信息**：与项目无关的闲聊或测试内容

## 注意事项

1. **保持简洁**：记录应精炼，避免冗余
2. **结构清晰**：使用标题、列表、代码块等格式
3. **及时更新**：信息变化时立即更新记录
4. **安全优先**：不记录敏感信息，必要时进行脱敏
5. **用户确认**：重要记录可询问用户是否需要保存

---

drf-resource-template 是独立的仓库，与 drf_resource 主框架仓库分离。

drf-resource-template 是基于 drf_resource 框架的脚手架模板，使用 cruft（基于 cookiecutter 的增强工具）实现一键项目生成。它提供了开箱即用的 Django 项目结构，集成了 drf_resource 框架的核心功能（Resource → ViewSet → Router 三层架构），并包含示例 App、配置架构、功能开关等完整模板。

---

# Writing Pipeline

## 规则

当 AI 完成以下任一操作后，**必须自动调用 `auto-review` skill** 执行审查修复闭环：

| 触发条件 | 操作类型 |
|----------|----------|
| 写入或修改 `.md` 文件 | `write_to_file` / `replace_in_file` |
| 写入或修改代码文件（`.py` `.sh` `.ps1` `.js` `.ts` `.java` `.go` `.rs` `.c` `.cpp` `.h` `.toml` `.yaml` `.yml` `.json` `.css` `.vue` `.tsx` `.jsx`） | `write_to_file` / `replace_in_file` |

## 执行流程

```
写完文件 → use_skill("auto-review") → 判断复杂场景 → (若复杂) use_skill("challenger")
```

### 第一步：auto-review 审查修复

按上述触发条件自动调用 `auto-review`。

### 第二步：复杂场景判断

auto-review 完成后，自动评估本次修改是否属于**复杂场景**。满足以下**任一条件**即视为复杂：

| 判断维度 | 复杂场景特征 |
|----------|-------------|
| 变更规模 | 涉及 3 个以上文件，或单文件变更超过 50 行 |
| 核心逻辑 | 修改涉及控制流（条件/循环）、错误处理、并发/异步逻辑 |
| Bug 修复 | 修复了运行时错误、逻辑缺陷、数据一致性问题 |
| 新增功能 | 添加了新的函数/方法/类/模块，或新增了外部接口 |
| 重构优化 | 重命名公共接口、提取模块、调整依赖关系、性能优化 |
| 关键路径 | 涉及认证、权限、数据持久化、支付、事务等关键业务逻辑 |
| 跨模块影响 | 变更影响多个模块之间的调用或数据流 |

**注意**：仅文档措辞修改、注释补充、格式调整、typo 修复等**不属于**复杂场景。

### 第三步：调用 challenger

判定为复杂场景后，**自动调用 `challenger` skill** 进行二次质疑审查：

```
use_skill("challenger")
```

调用时将本次修改的变更内容作为上下文传入，challenger 会根据变更类型（Bug 修复/新增功能/优化）选择对应质疑策略进行深度审查。

## 例外

以下情况跳过整条流水线：
- 用户明确说"不用审查"、"跳过审查"、"skip review"
- 单字符/标点修改

以下情况跳过 challenger（但不跳过 auto-review）：
- 用户明确说"不用质疑"、"跳过 challenger"
- 修改明确属于非复杂场景（见上表）

---

## [文档] 2026-07-07

### README.md 重构与 configuration.md 创建

- **删除** `README.md` 中 "⚠️ 项目状态" 警告章节（原第 15-20 行）
- **重写** `README.md`：从 738 行精简至 118 行
  - 保留：项目概述（含解决的问题/核心差异）、核心特性、快速开始（安装/配置/定义/调用/暴露API）、文档导航、许可证
  - 移除：详细使用指南（CacheResource/APIResource/自动发现/ViewSet路由/异步任务/批量请求/最佳实践/API文档配置）—— 这些内容已存在于 `docs/` 目录
  - 新增：文档导航表格，链接到 `docs/configuration.md`
- **创建** `docs/configuration.md`（341 行）：完整解释 `drf_resource/settings.py` 中全部 17 个配置项
  - 每个配置项包含：类型、默认值、使用场景、源码使用位置、代码示例
  - 分类：缓存 / 数据收集 / 认证 / HTTP客户端 / Celery / API文档
  - 底部含完整配置示例与相关文档交叉引用

### 质量审查结果

| 检查项 | 状态 |
|--------|------|
| 无占位符 (Q-01) | ✅ 通过 |
| 命令有输出 (Q-02) | ✅ 通过 |
| API 示例完整 (Q-03) | ✅ 通过 |
| 无推卸引用 (Q-04) | ✅ 通过 |
| 无模糊表述 (Q-05) | ✅ 通过 |
| 无空心章节 (Q-06) | ✅ 通过 |
| README 长度 ≤ 120 行 | ✅ 通过 (118 行) |
| 子文档链接可达 | ✅ 通过 |
| 跨引用一致性 | ✅ 通过 |

### 相关文件

- `README.md` — 重写后的项目入口文档
- `docs/configuration.md` — DRF_RESOURCE 配置参考
- `drf_resource/settings.py` — 配置项定义源头（5-28 行）